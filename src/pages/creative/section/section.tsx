import React from 'react';

export type SectionProps = {
  name: string,
  desc?: string,
  images?: string[],
  subSections?: SectionProps[],
}

export const Section = () => {
  return (
    <div>

    </div>
  )
};
